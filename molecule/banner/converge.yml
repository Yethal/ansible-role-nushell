---
- name: Converge
  hosts: all
  vars:
    disable_banner: true
    nu_users:
      - ansible
  tasks:
    - name: "Include yethal.nushell"
      ansible.builtin.include_role:
        name: yethal.nushell
