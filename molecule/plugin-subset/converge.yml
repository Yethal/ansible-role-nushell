---
- name: Converge
  hosts: all
  vars:
    plugins:
      - nu_plugin_query
    nu_users:
      - ansible
  tasks:
    - name: "Include yethal.nushell"
      ansible.builtin.include_role:
        name: yethal.nushell
